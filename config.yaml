server:
  port: 11611
  env: "dev"
  auth_api_key: "" # 仅内部 /internal/mcp，生产环境通过 SERVER_AUTH_API_KEY 注入

search:
  provider: bocha
  api_key: ""
  cx: ""

grpc:
  backend_target: "localhost:9090"

log:
  level: "debug"
